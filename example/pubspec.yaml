name: analytics_gen_example
description: Example project demonstrating analytics_gen usage
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.9.0
  flutter: ">=3.22.0"

dependencies:
  analytics_gen:
    path: ../
  flutter:
    sdk: flutter
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.26.2
