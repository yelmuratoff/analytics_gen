name: analytics_gen_example
description: Example project demonstrating analytics_gen usage
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  analytics_gen:
    path: ../

dev_dependencies:
  lints: ^6.0.0
