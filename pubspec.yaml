name: analytics_gen
description: Type-safe analytics event tracking with code generation from YAML configuration. Supports multiple providers, testing utilities, and export formats.
version: 0.1.0
repository: https://github.com/yourusername/analytics_gen

environment:
  sdk: ^3.7.2

dependencies:
  yaml: ^3.1.3
  path: ^1.9.1
  args: ^2.4.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
